import React from 'react';
import './Loading.css'; // Import CSS styles for the loading component

const Loading = () => {
  return (
    <div className="loading-overlay">
      <div className="loading-line"></div>
    </div>
  );
};

export default Loading;
